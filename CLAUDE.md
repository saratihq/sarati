# Sarati

The working rules live in **[AGENTS.md](AGENTS.md)** — one file, so every coding agent reads the same
thing. This file exists only because Claude Code loads `CLAUDE.md`, and it imports that one below.

Add rules to `AGENTS.md`, never here.

@AGENTS.md
