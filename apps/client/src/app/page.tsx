"use client";

import Layout from "@/components/Layout";

// Home ("/") — the one-page workflow surface; Layout hosts the dashboard and the step machine.
export default function Page() {
  return <Layout />;
}
