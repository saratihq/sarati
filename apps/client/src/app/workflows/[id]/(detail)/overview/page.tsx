"use client";

// The shell and workflow header live in the [id] layout; this page renders the overview screen.
import WorkflowOverviewPage from "@/components/WorkflowOverviewPage";

export default function Page() {
  return <WorkflowOverviewPage />;
}
