"use client";

import WorkflowSettingsPage from "@/components/WorkflowSettingsPage";

// Per-workflow settings + danger zone; the workflow context comes from the [id] layout.
export default function Page() {
  return <WorkflowSettingsPage />;
}
