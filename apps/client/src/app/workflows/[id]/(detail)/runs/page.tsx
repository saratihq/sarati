"use client";

// Run history for one workflow; inside the (detail) shell for the header + breadcrumb.
import WorkflowRunsPage from "@/components/WorkflowRunsPage";

export default function Page() {
  return <WorkflowRunsPage />;
}
